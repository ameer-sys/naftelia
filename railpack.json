{
  "$schema": "https://schema.railpack.com",
  "provider": "node,python",
  "buildAptPackages": ["libpq-dev"],
  "cmds": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && pip install -r backend/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn -w 4 -b 0.0.0.0:$PORT app:app"
  }
}
